name: test
channels:
  - psi4
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov

  - psi4=1.2
  - dftd3
    # Core
  - numpy
  - python
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.2.6
  - qcenegine
  - pydantic >=0.18.1


    # Pip-only installs
  - pip:
    - codecov
    - git+https://github.com/leeping/geomeTRIC#egg=geometric
